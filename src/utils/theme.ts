import {extendTheme} from 'native-base';

export const theme = extendTheme({
  colors: {
    gray: {
      dark: '#212121',
      medium: '#757575',
      light: '#DEDEDE',
      lightest: '#FAFAFA',
    },
    textBg: '#746A64',
    white: '#FFFFFF',
  },
});
